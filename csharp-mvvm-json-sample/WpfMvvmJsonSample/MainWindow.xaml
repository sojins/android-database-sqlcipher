<Window x:Class="WpfMvvmJsonSample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="노트 편집기" Height="450" Width="700">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Margin="0 0 0 12">
            <TextBlock Text="제목 (20자 제한)" FontWeight="SemiBold"/>
            <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" 
                     MaxLength="20"
                     Margin="0 4 0 0"
                     ToolTip="제목은 20자를 넘길 수 없습니다."/>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0 0 12 12">
            <TextBlock Text="본문 (300자 편집)" FontWeight="SemiBold"/>
            <TextBox Text="{Binding Body, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     MaxLength="300"
                     Margin="0 4 0 0"
                     Height="260"
                     TextWrapping="Wrap"
                     ToolTip="본문은 300자까지 입력 가능합니다."/>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="1" Margin="0 0 0 12">
            <TextBlock Text="본문 미리보기 (300자)" FontWeight="SemiBold"/>
            <Border BorderBrush="Gray" BorderThickness="1" Margin="0 4 0 0" Padding="8" Height="260">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock Text="{Binding PreviewBody}" TextWrapping="Wrap"/>
                </ScrollViewer>
            </Border>
        </StackPanel>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
            <Button Content="열기" Width="80" Command="{Binding LoadCommand}"/>
            <Button Content="저장" Width="80" Command="{Binding SaveCommand}"/>
            <Button Content="새로 작성" Width="80" Command="{Binding NewCommand}"/>
        </StackPanel>
    </Grid>
</Window>
